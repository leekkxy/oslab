1.bootsect加载到0x07c00处后，将自己先移动到0x90000处。这么做是因为system模块要被加载到0x10000处，会覆盖掉bootsect。解决方案：开机时直接将bootsect加载到0x90000处
2.setup将system再新移动到0地址处。因为在实模式下中断表占用了0处的位置，而setup读取各种参数还需要使用BIOS中断。
解决方案：可将中断表移动到其他位置
3.A20地址线的问题。开机后A20地址线被屏蔽。完全是为了兼容古老的8086程序。
解决方案：去掉屏蔽